% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meteo.R
\name{mfclim_get_data}
\alias{mfclim_get_data}
\title{Download archived meteorological data from the Météo-France "Données Climatologiques" API in one step}
\usage{
mfclim_get_data(
  token,
  client_auth,
  station,
  step,
  date_deb,
  date_fin,
  filename = "data.csv"
)
}
\arguments{
\item{token}{Character string. OAuth2 access token obtained with
\code{mfclim_get_token()} (optional if \code{client_auth} is provided).}

\item{client_auth}{Character string. Authentication string provided by the
Météo-France API portal (optional if \code{token} is provided).}

\item{station}{Character string. Station identifier.}

\item{step}{Character string. Time step of the requested data.}

\item{date_deb}{Character string. Start date in ISO 8601 \emph{UTC} format (see example). If \code{step = "6m"}, minutes values must be multiples of 6 (00, 06, 12, 18, 24, 30, 36, 42, 48, 54).}

\item{date_fin}{Character string. End date in ISO 8601 \emph{UTC} format. If \code{step = "6m"}, minutes values must be multiples of 6 (00, 06, 12, 18, 24, 30, 36, 42, 48, 54).}

\item{filename}{Character string. Name of the downloaded CSV file.}
}
\value{
A data.frame containing the climate data.
}
\description{
This is a wrapper function that retrieves data from the
Climatological Data API for a given station and period. The function can
either use an existing token or automatically request a new token
using client credentials.
}
\details{
The function:
\enumerate{
\item Obtains an access token if not provided
\item Submits a data order
\item Waits for the file to be generated
\item Downloads the CSV file
\item Reads the CSV file into R
}
}
\examples{
\dontrun{
data <- mfclim_get_data(
  client_auth = client_auth,
  station = "75114001",
  step = "1d",
  date_deb = "2020-01-01T00:00:00Z",
  date_fin = "2020-12-31T23:59:59Z"
)
}
}
\seealso{
\code{mfclim_get_token()},
\code{mfclim_order()},
\code{mfclim_download()}
}
