% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meteo.R
\name{mfclim_list_stations}
\alias{mfclim_list_stations}
\title{Get the list of Météo-France stations by department}
\usage{
mfclim_list_stations(
  token,
  client_auth,
  departement,
  step = c("1d", "1h", "6m"),
  parametre = NULL
)
}
\arguments{
\item{token}{Character string. OAuth2 access token obtained with
\code{mfclim_get_token()}.}

\item{client_auth}{Character string. Authentication string provided by the
Météo-France API portal. Used to automatically obtain a token if
\code{token} is not provided.}

\item{departement}{Character or numeric. French department code.}

\item{step}{Character string. Data frequency. One of:
\code{"1d"} (daily), \code{"1h"} (hourly), \code{"6m"} (6 minutes).}

\item{parametre}{Character string. Optional meteorological parameter used
to filter stations. One of:
\code{"precipitation"}, \code{"temperature"}, \code{"humidite"},
\code{"vent"}, \code{"pression"}, \code{"rayonnement"},
\code{"insolation"}, \code{"etat_mer"}, \code{"evapo"}.}
}
\value{
A data.frame containing the list of stations and their metadata.
}
\description{
Retrieve the list of available Météo-France stations for a given French
department and data frequency. The list can optionally be filtered by
meteorological parameter.
}
\details{
If \code{token} is not provided, the function will automatically
request a new token using \code{client_auth}.
}
\examples{
\dontrun{
stations <- mfclim_list_stations(
  client_auth = client_auth,
  departement = "75",
  step = "1d",
  parametre = "temperature"
)
}
}
